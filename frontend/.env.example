# Server-only Cloud Run/API origin. Never expose this to the browser.
OPSPILOT_API_TARGET=http://127.0.0.1:8000

# Public Cloudflare Turnstile site key (safe to expose). Secret stays in Secret Manager.
# NEXT_PUBLIC_TURNSTILE_SITE_KEY=
